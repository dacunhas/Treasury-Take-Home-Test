# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# secrets — the build-agent's GitHub PAT and any other local secrets
.secrets/
*.pat
github-pat.txt

# large / personal reference exports — keep out of the repo
*.pdf

# leftover sandbox test artifacts (cannot be deleted from some sandboxes)
# next.js / typescript generated artifacts
next-env.d.ts
*.tsbuildinfo

# env files — real keys live in .env.local; never commit them (.env.example is the template)
.env*
!.env.example
